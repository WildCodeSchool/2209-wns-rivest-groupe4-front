import LoginContainer from '../container/LoginContainer';

const LoginScreen = () => {
  return (
    <section>
        <h1>Login screen</h1>
        <LoginContainer />
    </section>
  )
}

export default LoginScreen